# vim:ft=ruby:

# This file contains formulas that should only be installed on linux distros. In case no exclusive
# formula is required, just leave the file empty
# For other formulas, see the files below:
#
# homebrew/required - formulas required to setup dotfiles
# homebrew/common - formulas that should be installed on both macOS and Linux boxes
# homebrew/macos - formulas that should only be installed on macOS boxes
