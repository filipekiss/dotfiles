# Youtube-dl default options

# Set the filename to safe video title format. This will result in VIDEO_TITLE.mp4
-o '%(title)s.%(ext)s'
--restrict-filenames

# Save video thumbnails next to video
# --write-thumbnail

# Save video description to a .description file
# --write-description

# display progress in console title bar (useful for background downloading)
--console-title

# skip youtube dash manifest
--youtube-skip-dash-manifest

# format options: Download best mp4 format available or any other best if no mp4 available
-f 'bestvideo[ext=mp4]+bestaudio[ext=m4a]/best[ext=mp4]/best'
