#!/usr/bin/env bash

DIR="${BASH_SOURCE%/*}"
if [[ ! -d "$DIR" ]]; then DIR="$PWD"; fi
source "${DIR}/utils/utils"

#try to lock screen, in order: osx, xubuntu (you may add more)
[[ ${IS_OSX} == ${YES} ]] && open -a ScreenSaverEngine
[[ ${IS_UBUNTU} == ${YES} ]] && xflock4
