#!/usr/bin/env zsh

local _wifi_ssid=$(/System/Library/PrivateFrameworks/Apple80211.framework/Versions/Current/Resources/airport -I | awk -F': ' '/ SSID/{print $2}')

local _symbol="₩:"

[[ $@ =~ "no-symbol" ]] && _symbol=""
[[ -n $_wifi_ssid ]] && echo ${_symbol}${_wifi_ssid} || exit 0
