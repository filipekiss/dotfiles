#!/usr/bin/env zsh
#
function is_dotfiles_bin() {
  [[ "$(basename $0 2>/dev/null)" == dotfiles ]] || return 1
}

if is_dotfiles_bin; then
    echo "is binary"
else
    echo "not binary"
    basename $0
fi
