# Suffix Aliases for ZSH

alias -s css=$EDITOR
alias -s html=$EDITOR
alias -s js=$EDITOR
alias -s json=$EDITOR
alias -s php=$EDITOR
alias -s scss=$EDITOR
alias -s styl=$EDITOR
alias -s svg=$EDITOR
alias -s twig=$EDITOR
